## 参考答案

1. 移动语义是 C++11 引入的一项特性，对于大型对象或包含资源的对象（如动态分配的内存、文件句柄等），复制构造函数可能会非常昂贵。移动语义允许对象的资源被“移动”到新对象，而不是复制，从而节省资源和时间。其主要作用是优化资源的利用，特别是在对象的复制操作中。
2. 移动语义通过移动构造函数和移动赋值运算符实现。在移动构造或移动赋值过程中，源对象的资源被“拿走”，并转移到目标对象，源对象变为无效状态。

## 背诵版

1. 移动语义是 C++11 引入的一项特性，对于大型对象或包含资源的对象（如动态分配的内存、文件句柄等），**拷贝构造函数 的成本可能会非常昂贵**。移动语义**允许对象的资源被“移动”到新对象，而不是复制，从而节省资源和时间。其主要作用是优化资源的利用，特别是在对象的复制操作中。**
2. 移动语义通过**移动构造函数和移动赋值运算符实现**。在移动构造或移动赋值过程中，**源对象的资源被“拿走”，并转移到目标对象，源对象变为无效状态。**
