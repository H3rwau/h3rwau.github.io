## 参考答案

MySQL的执行引擎主要负责查询的执行和数据的存储, 其执行引擎主要有`MyISAM`、`InnoDB`、`Memery` 等。

- `InnoDB`引擎**提供了对事务ACID的支持**，还提供了**行级锁和外键的约束**，是**目前MySQL的默认存储引擎**，适用于需要事务和高并发的应用。
- `MyISAM`引擎是早期的默认存储引擎，支持全文索引，但是**不支持事务**，也**不支持行级锁和外键约束**，适用于快速读取且数据量不大的场景。
- `Memery`就是将**数据放在内存中，访问速度快**，但**数据在数据库服务器重启后会丢失**。